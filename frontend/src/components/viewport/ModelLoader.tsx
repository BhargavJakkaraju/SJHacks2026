"use client";

export default function ModelLoader() {
  return (
    <div className="rounded-lg border border-zinc-800 bg-zinc-950/60 p-3 text-xs text-zinc-400">
      Model loader: waiting for generated `GLB` URL.
    </div>
  );
}
