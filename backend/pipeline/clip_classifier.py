async def classify_sketch(sketch_data_url: str) -> tuple[str, float]:
    """
    Stub for CLIP-style sketch classification.
    Returns (label, confidence in [0, 1]).
    """
    _ = sketch_data_url
    return ("character", 0.73)
